# AGENTS.md

## Commit policy

- Do not create commits unless the user explicitly asks for a commit in that session.
- Preparing or suggesting a commit is allowed, but the actual `git commit` must only happen after an explicit user request.
